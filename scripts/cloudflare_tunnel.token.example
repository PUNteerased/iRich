# Cloudflare Tunnel token (NEVER commit this file)
# Paste the long token from Zero Trust → Tunnels → Install connector (Windows)
#
# Example line (one line only, no quotes):
# eyJhIjoi....very-long-token....
